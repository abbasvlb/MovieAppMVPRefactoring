# MovieAppMVPRefactoring
Step by Step code reference to understand MVP by refactoring a move app.

You need to add following in your local properties. Replace ? mark with "The Movie DB" api key.

default.key=?
